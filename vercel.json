{ 
  "routes": [
    {
      "src": "/(.*)",
      "dest": "/(.*)",
      "methods": ["GET", "POST", "PUT", "DELETE", "PATCH", "OPTIONS", "FETCH"],
      "headers": {
        "Access-Control-Allow-Origin": "*"
      }
    }
  ]
}
